# Database Configuration for viz-streamlit-ahu
# Copy this file to .env and update with your credentials

# Data Source Mode
DATA_SOURCE_MODE=parquet  # Options: parquet, database

# ==============================================
# Database Configuration (for database mode)
# ==============================================
# The app supports two database connection methods:
# 1. Direct PostgreSQL connection (DB_* variables) - default for local development
# 2. Pgbouncer connection pool (PGB_* variables) - recommended for production
#    Takes precedence if ANY PGB_* is set

# Option 1: Direct PostgreSQL connection (DB_* variables)
DB_HOST=localhost
DB_PORT=5433
DB_NAME=ahu_monitoring
DB_USER=postgres
DB_PASSWORD=admin

# Option 2: Pgbouncer connection (PGB_* variables)
# Uncomment to use pgbouncer instead of direct DB connection:
# PGB_HOST=localhost
# PGB_PORT=6432
# PGB_NAME=ahu_read
# PGB_USER=postgres
# PGB_PASSWORD=admin
#
# Note: You only need to set the PGB_* variables that differ from defaults.
#       If any PGB_* is set, all DB_* variables will be ignored.
#       Defaults: PGB_HOST=pgbouncer, PGB_PORT=6432, PGB_NAME=ahu_read, PGB_USER=postgres
#       PGB_PASSWORD will inherit from DB_PASSWORD if not set.

# Parquet File Paths (for parquet mode - Windows paths default)
# Override these if your files are in a different location
# AHU_HISTORY_DIR=C:\Users\User\Desktop\history
# AHU_RESULT_BASE=C:\Users\User\Desktop\ahu_app_results

# Optional: OpenAI API Key for GPT insights
# OPENAI_API_KEY=your_api_key_here
